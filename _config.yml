# Site settings
title: Athena
description: A GraphQL Object Storage Webservice Backed by OpenStack Swift and Hadoop HDFS
# baseurl: "/particle" # the subpath of your site, e.g. /blog
# url: "localhost:3000" # the base hostname & protocol for your site e.g. http://willianjusten.com.br

# User settings
username: Athena
user_description: A GraphQL Object Storage Webservice Backed by OpenStack Swift and Hadoop HDFS
# user_title: A GraphQL Object Storage Webservice Backed by OpenStack Swift and Hadoop HDFS
# email: anon@anon.com
# twitter_username: lorem_ipsum
# github_username:  lorem_ipsum
# gplus_username:  lorem_ipsum

# Google-analytics
google-analytics:
  id: ""

# exclude: ['package.json', 'src' , 'node_modules']

# doc-site configs:
# build settings
markdown: kramdown
sass:
  load_paths:
    - _sass
    - node_modules
exclude: []

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

defaults:
  -
    scope:
      path: "node_modules/**"
    values:
      sitemap: false

# site-specific configuration
BASE_PATH: https://qubitpi.github.io/athena
ASSET_PATH: /assets
